# --- LLM extractor (required: pick ONE) ------------------------------------
# Option A: Claude (native Anthropic). Auto-detected when ANTHROPIC_API_KEY is set
# and no OpenRouter key is. The cheapest capable Claude is the default.
# ANTHROPIC_API_KEY=sk-ant-...
# COGNIFY_ANTHROPIC_MODEL=claude-3-5-haiku-latest

# Option B: any OpenAI-compatible endpoint (OpenRouter, OpenAI, vLLM, Ollama).
OPENROUTER_API_KEY=sk-or-...
# COGNIFY_LLM_BASE=https://openrouter.ai/api/v1
# COGNIFY_LLM_MODEL=openai/gpt-4o-mini
# COGNIFY_LLM_KEYENV=OPENROUTER_API_KEY      # which env var holds the key

# Force a provider explicitly (else auto-detected): openai | anthropic
# COGNIFY_LLM_PROVIDER=anthropic

# --- backend selection ------------------------------------------------------
# local  = ChromaDB + networkx, zero external services (default)
# neo4j  = TurboVec + Neo4j, for a shared/server graph
COGNIFY_BACKEND=local

# --- storage ----------------------------------------------------------------
# COGNIFY_DATA_DIR=~/.cognify

# --- Neo4j (only when COGNIFY_BACKEND=neo4j) -------------------------------
# NEO4J_URI=neo4j://localhost:7687
# NEO4J_USER=neo4j
# NEO4J_PASSWORD=changeme
# COGNIFY_NEO4J_ENV_FILE=/path/to/other/.env   # optional: read creds from a file
